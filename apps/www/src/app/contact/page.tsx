export default function ContactPage() {
  return (
    <main className="summary" aria-labelledby="contact-title">
      <article>
        <h1 id="contact-title">Contact</h1>
        <p>
          Public support and legal contact details will be finalized before launch. In-app report
          and account support flows are tracked in later implementation issues.
        </p>
      </article>
    </main>
  );
}
