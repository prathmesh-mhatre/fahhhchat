export default function GuestEntryPage() {
  return (
    <main className="chat-shell" aria-labelledby="guest-title">
      <section className="chat-panel">
        <div className="topbar">Guest setup</div>
        <div className="entry">
          <p className="eyebrow">Next slice</p>
          <h1 id="guest-title">Age and legal gate</h1>
          <p>
            The guest legal acceptance flow is tracked separately so this shell stays focused on
            the first monorepo issue.
          </p>
        </div>
      </section>
    </main>
  );
}
